name: Demo Scheduler 
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "Current server time is ${date}"
